# astrox-helm-chart 操作流程文档

> 本文档基于仓库实际代码（Jenkinsfile / pipline.groovy / setting.groovy / Helm 模板）整理，描述本项目从「接入服务」到「发布、校验、回滚」的完整流程，供运维与研发使用。
>
> **Astrox改造说明**：业务代码仓库统一走 GitHub（`AstroXExchange`组织）；环境收窄为 **test/dev/uat/prod**（`sit`已删除）；build 只保留 2 次（`test`共享构建 + `prod`独立构建），`dev`/`uat`改成纯部署，不再各自 build。**Jenkins 地址切换到 `https://jenkins.astroxs.com/` 不在本次改造范围内，需要在 Jenkins 侧自行配置，本仓库代码不涉及。**
>
> **对齐真实基础设施改造说明（第二轮）**：参照Astrox真实在跑的Jenkins job校对后，本仓库又做了一轮改造，但**保留了Helm部署机制**（没有照抄真实job的`kubectl set image`纯kubectl部署）：Jenkins agent 从固定静态 `node(label)` 改成 K8s 动态pod agent（`podTemplate`+`node(POD_LABEL)`）；构建工具从 `docker build/push` 改成 `buildah`+显式 `aws ecr login`；镜像仓库地址换成真实 AWS ECR（test/dev/uat共用账号 `178092210163`，prod待补充）；新增飞书（Lark）成功/失败通知；`namespaces` 拆分出一个新的 `env_tier` 字段（见第三节）；新增 `java17_maven` project_type。

---

## 一、项目简介

`astrox-helm-chart` 是一套 **Jenkins + Helm 的微服务持续交付编排仓库**，负责把 GitHub 上的各业务代码仓库构建成镜像并部署到 Kubernetes（AWS EKS）。它本身不是单个服务的 chart，而是「一份通用 Helm 模板 + 一套 Jenkins 流水线 + 一份服务清单配置」，用一套模板支撑多环境、多语言运行时的服务发布。

核心设计：

- **配置即清单**：每个服务的部署参数集中写在对应环境的 `setting.groovy` 里，新增/修改服务只改配置，不动模板。
- **模板渲染**：用 `envsubst` 把 `template_*.values.yaml`、`template.Chart.yaml` 中的 `${变量}` 替换成实际值，生成最终的 `values.yaml` / `Chart.yaml`。
- **2次build模型**：`test/` 共享构建一次镜像供 dev/uat 部署复用；`prod/` 独立构建一次。dev/uat 之间不再各自重复 build。
- **发布失败自动回滚**：`kubectl rollout status` 检测失败后流水线自动执行 `rollout undo`。
- **K8s动态pod agent**：所有环境的Jenkins agent都是按需起的K8s pod（`podTemplate`），不是固定的静态node，agent的K8s cloud名和容器镜像地址由各环境 `setting.groovy` 的 `private.jenkins_cloud`/`private.agent_image` 决定。

技术栈：Jenkins（脚本式 Pipeline + K8s动态pod agent）、Helm v3、Kubernetes（Deployment/StatefulSet）、buildah + AWS ECR、GitHub、AWS EFS（NFS 挂载配置与日志）、SkyWalking（Java 链路追踪）、飞书（Lark）通知。

---

## 二、仓库目录结构

```
astrox-helm-chart/
├── Dockerfile_dir/                # 各运行时的 Dockerfile 模板
│   ├── java8_Dockerfile
│   ├── newexchange_java8_Dockerfile
│   ├── java17_maven_Dockerfile
│   ├── nginx_Dockerfile
│   ├── nodejs_Dockerfile
│   ├── nodejs_explore_Dockerfile
│   ├── go_Dockerfile
│   └── python_Dockerfile
├── customize_server.conf          # nginx 类前端的 server 配置
├── README.md
├── chart_templates/                # helm chart模版共享目录（唯一canonical来源，见3.1节）
│   ├── template.Chart.yaml
│   ├── template_<type>.values.yaml# 8种project_type各一份（含新增java17_maven）
│   └── templates/
│       ├── deployment.yaml
│       ├── service.yaml
│       ├── apisixroute.yaml       # 生成ApisixRoute CRD（已替换原生Ingress，见3.1节）
│       ├── hpa.yaml
│       └── _helpers.tpl
├── test/                          # 两个独立job：共享构建（checkout+build+push）+ 独立部署
│   ├── astrox.Jenkinsfile         # 构建job
│   ├── pipline.groovy
│   ├── deploy.Jenkinsfile         # 部署job（新增，跟构建job完全独立）
│   ├── deploy_pipline.groovy      # 部署job只负责解析SPECIFY_TAG/IMAGE_TAG
│   └── setting.groovy             # build字段+部署字段都写在同一个子字典里（因为两个job共用同一份文件）
├── dev/  uat/                      # 纯部署环境，结构相同（见下）
│   ├── astrox.Jenkinsfile             # 部署主入口（接收SPECIFY_TAG/IMAGE_TAG参数，Update values.yaml阶段运行时从chart_templates/拷贝chart文件）
│   ├── pipline.groovy             # 只负责解析SPECIFY_TAG/IMAGE_TAG，决定用手动tag还是自动查ECR最新tag
│   └── setting.groovy             # 只需要部署相关字段
└── prod/                          # 独立构建+部署（不变）
    ├── astrox.Jenkinsfile
    ├── pipline.groovy
    └── setting.groovy             # build字段+部署字段都要写
```

> 注意：`./<env>/` 目录本身就是 Helm chart 目录。渲染后 `Chart.yaml`、`values.yaml` 与 `templates/` 同级，`helm upgrade` 直接指向 `./astrox-helm-chart/<env_tier>`（`env_tier` 是环境目录名，跟真正的k8s namespace即 `namespaces` 字段是两回事，见第三节）。`dev`/`uat`/`prod`/`test`部署job本身不再各自存放`template.Chart.yaml`/`template_*.values.yaml`/`templates/`——这些文件只在仓库根目录`chart_templates/`维护一份，部署job跑的时候现拷贝一份到自己环境目录再渲染（见2.1节）。

### 2.1 `chart_templates/` 共享机制（去重合并，本轮新增）

以前 `dev`/`uat`/`prod` 各自维护一份 `template.Chart.yaml`/`template_*.values.yaml`/`templates/`，改一处chart结构（比如加个资源限制字段、改ingress规则）要同步改三四份，实际已经产生过真实bug：`uat`/`prod`的`templates/hpa.yaml`内容被重复粘贴了两次导致helm渲染报错；`prod`的values镜像tag拼法跟`dev`/`uat`不一致；`prod`的`hosts:`字段格式是裸标量不是YAML列表，ingress规则渲染错误；`uat`的HPA副本数硬编码跟`dev`不一致。这些逐份复制维护产生的漂移现已合并修复。

现在改成仓库根目录 `chart_templates/` 维护**唯一一份**canonical文件，各环境部署job（`dev`/`uat`/`prod`/`test`部署job）的 `Update values.yaml` 阶段最前面先执行：
```bash
rm -rf ${chart_name}/${env_tier}/templates
mkdir -p ${chart_name}/${env_tier}/templates
cp ${chart_name}/chart_templates/template.Chart.yaml ${chart_name}/${env_tier}/
cp ${chart_name}/chart_templates/template_*.values.yaml ${chart_name}/${env_tier}/
cp ${chart_name}/chart_templates/templates/* ${chart_name}/${env_tier}/templates/
```
再执行原有的 `envsubst`/`sed` 渲染逻辑（逻辑本身不变）。这样每次pipeline跑的时候现装现用，git里只留一份文件，同时满足Helm要求chart目录必须自包含的前提（不能直接引用仓库外的共享目录当chart source）。以后要改镜像资源限制、ingress规则、HPA副本数上下限之类的chart结构，只改 `chart_templates/` 一处，四个环境下次部署时都会吃到新版本。

---

## 三、环境与命名空间

| 环境 | env_tier(固定) | namespace(默认值,服务级可覆盖) | Jenkins cloud | 镜像仓库(AWS ECR) | KUBECONFIG | EFS 区域 | 资源(默认 limits) | 是否build | 是否deploy |
|---|---|---|---|---|---|---|---|---|---|
| test | `test` | `test`（也用于ECR镜像path） | `kubernetes` | `178092210163.dkr.ecr.ap-southeast-1.amazonaws.com` | `/data/kubeconfig/ofc` | ap-southeast-1 | 1C / 2G | ✅ 共享build（`astrox.Jenkinsfile`） | ✅ 独立部署job（`deploy.Jenkinsfile`，新增） |
| dev | `dev` | `dev` | `kubernetes` | 同 test（`178092210163...`） | `/data/kubeconfig/ofc` | ap-southeast-1 | 1C / 4G | ❌ 只部署 | ✅ |
| uat | `uat` | `uat` | `pre-eks` | 指向test/dev共用registry（`178092210163...`），**不是**uat自己账号`696000197734`，见下方跨账号说明 | `/data/kubeconfig/ofc` | ap-southeast-1 | 同 dev 量级 | ❌ 只部署 | ✅ |
| prod | `prod` | `prod25` | 待补充（`FILL_IN_PROD_JENKINS_CLOUD`） | `<prod账号>.dkr.ecr.<region>.amazonaws.com`（跟其它环境不同账号，待填） | `/data/kubeconfig/prod` | us-west-2 | 2C / 6G，默认开 autoscaling | ✅ 独立build | ✅（跟build同一个job） |

> test的构建job和部署job是**两个完全独立的Jenkins job**，只是共用同一份`test/setting.groovy`——跟prod（build+deploy在同一个job里）不一样，也跟dev/uat（部署job要跨文件读`test/setting.groovy`的namespaces）不一样，test部署job直接读自己所在文件即可，见5.1.1节。

> **`env_tier` 与 `namespaces` 的区别（重要，第二轮改造新增）**：
> - `env_tier`：**固定值**，等于环境目录名（`dev`/`uat`/`prod`），只用来定位chart源码子目录（`${chart_name}/${env_tier}`）和node taint/toleration，**不可被服务配置覆盖**。
> - `namespaces`：真正的k8s namespace，**服务级可覆盖**（写法同 `kind_name`/`limits_cpu` 等字段：`(code_info."${micro_key}".namespaces) ?: (code_info.private.namespaces)`），因为真实环境里同一环境下不同业务线用不同namespace（如 `test-product`、`test-funds`、`uat-match`）。不需要覆盖时缺省沿用环境默认值。
> 这两者以前是同一个字段，会导致"改了服务的k8s namespace，chart源码目录路径也跟着变"的错误联动，现已拆开。

> **TODO(部署前必须填)**：`dev`/`prod` 两个环境的真实ECR地址仍是占位符（`prod/setting.groovy` 的 `FILL_IN_ECR_REGISTRY_PROD.dkr.ecr.FILL_IN_REGION.amazonaws.com`），需要换成真实 ECR 地址；`prod/setting.groovy` 的 `jenkins_cloud`/`agent_image` 也还是占位符，需要用户核实真实值。构建走 buildah，push前会显式 `aws ecr get-login-password | buildah login`（不是docker免登录那一套）。

> **uat跨AWS账号前提（必须由用户在AWS侧确认/配置）**：test/dev共用ECR账号 `178092210163`，uat是独立账号 `696000197734`。`uat/setting.groovy` 的 `docker_repository_url` 已经代码侧改成指向test/dev共用registry（而不是uat自己账号），这样uat才能部署 `test` job构建产出的同一个镜像tag——但这要求test/dev账号下那个ECR仓库的仓库策略（repository policy）显式允许uat账号的角色执行 `ecr:GetDownloadUrlForLayer`/`ecr:BatchGetImage` 等只读权限。**这是AWS侧的基础设施前提，不是代码能解决的，务必确认好，否则uat部署会因为拉不到镜像而失败。**

> 共同参数都写在各环境 `setting.groovy` 的 `private` 块里（镜像仓库、`aws_region`、`jenkins_cloud`、`agent_image`、`lark_webhook_url`、namespace、默认 CPU/内存、node_select、KUBECONFIG、NFS 地址等），服务级参数会覆盖 `private` 的同名项。
>
> **镜像path注意**：dev/uat 的 `image_url` 固定拼成 `${docker_repository_url}/test/${app_name}`（去 `test` 共享仓库路径下拉镜像），跟自己的 k8s namespace（`dev`/`uat` 的 `namespaces` 字段）是两回事，不要混淆。

---

## 四、支持的运行时类型（project_type）

`project_type` 决定用哪个 Dockerfile 和哪条打包命令（`test`/`prod` 构建时使用；dev/uat 部署时仍需要这个字段，因为要选对 `template_<type>.values.yaml`）：

| project_type | 用途 | 打包方式（典型） |
|---|---|---|
| `java8` | Spring Boot 等 Java 服务 | gradle，`add_java_jar` 注入 jar |
| `newexchange_java8` | 交易所多模块 Java | maven `-pl <模块> -am -U` |
| `java17_maven`（新增，对齐真实基础设施） | JDK17+Maven 服务 | `mvn -pl <模块> -am package`，运行时基础镜像 `eclipse-temurin:17-jre-jammy` |
| `nginx` | 前端静态站（构建后用 nginx 托管） | yarn/npm/pnpm build，配 `customize_server.conf` |
| `nodejs` | Node 服务 | `node_ins` 指定的 yarn/npm build |
| `nodejs_explore` | 特殊 Node 前端 | 在 Dockerfile 注入 `namespaces` |
| `go` | Go 服务 | 在镜像构建阶段处理 |
| `python` | Python 服务 | 见 requirements.txt |

> `java8`/`newexchange_java8` 两个既有类型未做任何改动（现有服务继续用不受影响）；`java17_maven` 是本轮新增，参照Astrox真实在跑job（JDK17+Maven、`eclipse-temurin:17-jre-jammy`运行时）补上。

---

## 五、整体发布流程

### 5.1 test（共享构建）

`test/astrox.Jenkinsfile` 依次执行：

0. **Resolve build agent**：先用一个内置静态agent clone一次配置仓库，只读出 `test/setting.groovy` 里的 `private.jenkins_cloud`/`private.agent_image`，然后立即销毁——真正的checkout+build在下面的K8s动态pod agent里重新做一次。
1. **`podTemplate(cloud: jenkins_cloud, ...)` 起一个K8s动态pod**（容器镜像=`agent_image`），后续所有stage都在这个pod里跑：
   - **clone helm chart**：`git clone astrox-helm-chart`，拉取本编排仓库自身。
   - **Check info（读配置）**：以 Jenkins 任务名 `JOB_BASE_NAME` 去 `test/setting.groovy` 查服务的 build 字段（`github_url`/`project`/`project_type`/`maven_ins`等），查不到直接 `exit 1`。
   - **load `test/pipline.groovy`**：
     - **Check helm project code**：按 `BRANCH_TAG` 从 GitHub checkout 业务代码，取 8 位短 `commit_id`；
     - **Build/Push**：`cp Dockerfile_dir/<type>_Dockerfile ./Dockerfile` → 按 `project_type` 打包 → `aws ecr describe-repositories || aws ecr create-repository`（仓库不存在则创建）→ `aws ecr get-login-password | buildah login` → `buildah bud`/`buildah push`，tag 规则见第六节；
     - **Print image tag**：打印出这次构建的完整镜像 tag。
   - 构建成功/失败都会往飞书webhook（`private.lark_webhook_url`）发一条通知。
2. test/dev/uat 对应部署job的 `SPECIFY_TAG` **不勾选直接构建即可**——部署job会自动去ECR查这个服务最新一次push的tag（见5.1.1/5.2节），不用再人工复制；打印出来的tag只在需要回滚/部署指定历史版本时才勾选`SPECIFY_TAG`并填到`IMAGE_TAG`。

### 5.1.1 test（独立部署job，新增）

`test/deploy.Jenkinsfile`是test目录下**新增的第二个job**，跟上面5.1节的构建job完全独立、互不影响——test环境现在也能真正helm部署了（之前误以为test故意设计成"只构建不部署"，已纠正）。流程结构跟5.2节的dev/uat纯部署模式一致（`SPECIFY_TAG`+`IMAGE_TAG`参数、Update values.yaml→helm upgrade→rollout status自动回滚→飞书通知），差异点：

- **agent镜像不同**：读的是`test/setting.groovy`里新增的`private.deploy_agent_image`字段（只需要helm/kubectl/awscli），跟构建job用的`private.agent_image`（JDK17/Maven/buildah）是两个不同的镜像，互不干扰。
- **namespaces不用跨文件查**：dev/uat部署job要额外读一次`test/setting.groovy`才能拼出ECR镜像路径（见5.2节），而test部署job和test构建job本来就是同一份`test/setting.groovy`，`namespaces`直接在本文件内用`(code_info."${micro_key}".namespaces) ?: (code_info.private.namespaces)`解析即可，不需要`test_namespaces`那层cross-file查找。
- **配置字段合并在同一个子字典**：`test/setting.groovy`每个服务的子字典现在build字段（`github_url`/`project_type`等）和部署字段（`replicas`/`http_port`/`ingress_hosts`等）写在一起，因为两个job读的是同一个文件同一个key。

### 5.2 dev / uat（纯部署）

`<env>/astrox.Jenkinsfile` 依次执行：

0. **Resolve deploy agent**：同上，先用静态agent读出 `jenkins_cloud`/`agent_image`。
1. **`podTemplate` 起K8s动态pod**，后续stage都在pod里跑：
   - **clone helm chart**：同上，拉取编排仓库自身（不再拉业务代码）。
   - **Check info（读配置）**：查 `<env>/setting.groovy` 的部署字段（`app_name`/`project`/端口/ingress/资源等），读出 `env_tier`（固定）和 `namespaces`（服务级可覆盖，见第三节）；同时额外读一次 `test/setting.groovy`，取该服务在test环境下的 `namespaces` 覆盖值（记为 `test_namespaces`，是test构建时实际push的ECR路径段，不是字面量 `"test"`），拼出 `image_url = ${docker_repository_url}/${test_namespaces}/${app_name}`。
   - **load `<env>/pipline.groovy`**：解析 `params.SPECIFY_TAG`——不勾选（默认）则跑 `aws ecr describe-images` 自动取该服务ECR仓库里最新push的tag；勾选了则要求 `params.IMAGE_TAG` 必须非空并直接用（用于回滚/指定版本），设成 `env.image_tag`（不再 checkout 业务代码、不再 build/push）。
   - **Update values.yaml（模板渲染）**：
     ```bash
     cd ${chart_name}/${env_tier}
     envsubst < template_${project_type}.values.yaml > values.yaml
     # websocket_port=null 时删掉 websocket 端口块
     envsubst < template.Chart.yaml > Chart.yaml && rm -fr template_*
     cd templates && sed -i 's/{project}/${project}/g' _helpers.tpl hpa.yaml
     # no_ingress=true 时删除 apisixroute.yaml
     ```
     values.yaml 里的 `image:` 行现在统一是 `${image_url}:${image_tag}`（不再自己拼 `BRANCH_TAG`/`commit_id`/`BUILD_ID`，因为这次部署用的是 `test` job 已经构建好、且已经自动/手动解析出tag的镜像）。
   - **ofc helm upgrade（发布）**：`helm upgrade ${app_name} --install -n ${namespaces} ./${chart_name}/${env_tier}`（注意：chart目录路径用 `env_tier`，`-n` 后面的k8s namespace用 `namespaces`——两者不再是同一个值）。
   - **update helm chart（校验+自动回滚）**：`kubectl rollout status`，失败则打印日志并 `rollout undo`。
   - 成功/失败都往飞书webhook发通知。

### 5.3 prod（独立构建+部署）

流程结构同test+dev的合体：Resolve agent → podTemplate起pod → clone → Check info（build字段+部署字段都读，同样有 `env_tier`/`namespaces` 拆分）→ load `prod/pipline.groovy`（checkout GitHub业务代码+buildah build+push）→ Update values.yaml → helm upgrade（带 `--force`）→ 校验回滚 → 飞书通知。这是模型里唯一的「第2次build」，也是唯一build+deploy在同一个job里的环境——因此 `prod/setting.groovy` 的 `agent_image` 需要同时具备构建工具链（JDK17/Maven/buildah）和部署工具链（helm/kubectl/awscli）。

> 一句话概括模型：**test 构建一次镜像 → test（独立部署job）/dev/uat 部署时自动去ECR取最新tag做纯部署（`SPECIFY_TAG`不勾选即可，不用人工复制，需要回滚才勾选并手动填`IMAGE_TAG`）；prod 自己独立构建+部署，是第二次build。所有job的agent都是K8s动态pod，不是固定静态节点。**

---

## 六、镜像与版本规约

`test`/`prod` 构建时打的 tag：

```
${image_url}:${分支去掉斜杠前缀}-${commit_id}-${BUILD_ID}
例：<ECR地址>/test/auth-svc:dev-1a2b3c4d-128（<ECR地址>就是 setting.groovy 里 docker_repository_url 填的那个真实ECR地址）
```

- `分支去掉斜杠前缀`：如分支 `release/1.2` 取 `1.2`，普通分支如 `dev` 直接用 `dev`；
- `commit_id`：业务代码 8 位短哈希；
- `BUILD_ID`：Jenkins 构建号，保证每次构建 tag 唯一、可追溯。

test(部署job)/dev/uat 部署时不再自己拼 tag：`params.SPECIFY_TAG` 不勾选（默认）则自动查ECR最新push的tag，勾选则要求`params.IMAGE_TAG`非空并直接用该值（回滚/指定版本）。

Chart 侧：`Chart.yaml` 的 `appVersion` 写入 `commit_id`，`version` 固定 `1.0.1`；Deployment 模板 `revisionHistoryLimit: 10`，即 K8s 保留最近 10 个版本可供回滚。

---

## 七、配置说明（setting.groovy）

结构为一个 JSON：顶层 `private` 是公共默认值，其余每个 key 是一个服务（key 必须等于 Jenkins 任务名）。服务级同名参数会覆盖 `private`。**注意 build 字段和部署字段现在分家了：**

**`test/setting.groovy`（build字段+部署字段，写在同一个子字典里，因为构建job和部署job共用同一份文件）：**

| 参数 | 含义 |
|---|---|
| `github_url` | 业务代码仓库地址（GitHub），构建job用 |
| `project_type` | 运行时类型（见第四节），构建/部署job都要用 |
| `project` | 代码内模块名（打包、jar 路径用，同时也是helm `{project}.xxx`命名前缀），构建/部署job都要用 |
| `app_name` | 服务名 / 镜像名 / Helm release 名，构建/部署job都要用 |
| `node_ins` / `maven_ins` / `gradle_ins` | 各自的构建命令，构建job用 |
| `nodejs_version` | Node 版本，构建job用 |
| `replicas` | 副本数，部署job用（新增） |
| `http_port` / `actuator_port` / `websocket_port` | 端口，部署job用（新增） |
| `ingress_hosts` / `ingress_paths` | 域名与路径，部署job用（新增） |
| `no_ingress` | `true` 时不生成 `apisixroute.yaml`（内部服务），部署job用（新增） |
| `namespaces`（可选） | 服务级k8s namespace覆盖，不填则用 private 默认 `"test"`；构建job push镜像和部署job拉镜像/建namespace都用这个值，两个job保持一致 |
| `min_replicas`/`max_replicas`（可选，新增） | HPA副本数上下限，不填则用 private 默认 |

**`dev/uat/setting.groovy`（部署相关）：**

| 参数 | 含义 |
|---|---|
| `app_name` | 服务名 / Helm release 名 / k8s 资源名 |
| `project` | 同 test 里的值，helm `{project}.xxx`命名前缀要用 |
| `project_type` | 运行时类型（决定用哪个 `template_<type>.values.yaml`） |
| `replicas` | 副本数 |
| `http_port` / `actuator_port` / `websocket_port` | 端口 |
| `ingress_hosts` / `ingress_paths` | 域名与路径 |
| `no_ingress` | `true` 时不生成 `apisixroute.yaml`（内部服务） |
| `skywalking_enabled` | Java 服务是否注入 SkyWalking 探针 |
| `limits_cpu/mem`、`requests_cpu/mem` | 资源（不填则用 private 默认） |
| `nfs_server` / `log_nfs_server` | 配置/日志 EFS 地址（不填用 private 默认） |
| `namespaces`（可选） | 服务级k8s namespace覆盖，不填则用 private 默认（如 `test-product`/`uat-match` 这种按业务线区分的场景） |
| `min_replicas`/`max_replicas`（可选，新增） | HPA副本数上下限，不填则用 private 默认（此前硬编码在各环境`template_*.values.yaml`里，现在走跟`limits_cpu`一样的JSON字段覆盖机制） |

**`prod/setting.groovy`**：build字段+部署字段都要写在同一个子字典里（prod自己做完整的build+deploy），字段范围=上面test表+dev/uat表的并集，同样支持`min_replicas`/`max_replicas`。

**private 公共参数：** `docker_repository_url`、`chart_name`、`namespaces`（环境默认k8s namespace，服务级可覆盖）、`env_tier`（固定，等于环境目录名，不可覆盖，见第三节）、`aws_region`（ECR/buildah登录用）、`jenkins_cloud`（K8s动态pod agent的cloud名）、`agent_image`（K8s动态pod agent的容器镜像；仅test/prod构建时用JDK17/Maven/buildah规格，test部署job另有独立的`deploy_agent_image`）、`lark_webhook_url`（飞书通知webhook地址）、默认资源、`node_select`、`KUBECONFIG`、`nfs_server`、`log_nfs_server`、`add_java_jar`、`gradle_ins`、`kind_name`(默认 deployment)、`min_replicas`/`max_replicas`(新增，各环境默认值：dev=`2`/`10`，uat=`1`/`100`，prod=`2`/`10`，test=`1`/`2`) 等。

> **ingress → ApisixRoute 自动化改造（2026-09-08，已落地）**：
>
> 背景核实过程：`apisix`网关（3副本）、`apisix-ingress-controller`（2副本，2/2 Running）、`apisix-etcd`（3节点集群）全部健康；`kubectl get ingressclass`确认存在`apisix`这个class（`CONTROLLER: apisix.apache.org/apisix-ingress-controller`，`PARAMETERS`指向`GatewayProxy.apisix.apache.org/apisix-config`，是走Gateway API风格的2.x版本controller）。进一步核实集群里真实生效的`ApisixRoute`（`kubectl get apisixroute -A -o json`，抓到`rocketmq-dashboard`/`account-gateway`/`quotes-api`×2/`cloud-gateway`×2/`risk-engine`/`xxl-job-ui`共8条`enable:true`的路由），逐条查看`proxy-rewrite`插件的`config`内容，全部是`headers.set`（`X-Forwarded-Host`/`Port`/`Proto`/`Ssl`）+`host`字段改写，**没有一条出现`uri`字段**——证明原来`nginx.ingress.kubernetes.io/rewrite-target`这个注解从一开始就是nginx专用语法，apisix从不认、被静默忽略，是彻底的死代码；而集群真正在用的是`proxy-rewrite`插件的header改写能力。
>
> 据此已完成落地：
> 1. `chart_templates/templates/ingress.yaml`（生成原生k8s `Ingress`）已删除，替换成`chart_templates/templates/apisixroute.yaml`（生成`apisix.apache.org/v2` `ApisixRoute` CRD），字段结构照抄上面核实到的真实集群路由（`spec.ingressClassName`+`spec.http[].match.hosts/paths`+`spec.http[].backends[].serviceName/servicePort`+`spec.http[].plugins[]`）。
> 2. 每个服务的每个`ingress_hosts`都自动带上`proxy-rewrite`插件的header改写（`X-Forwarded-Host`=该host、`Port`=`443`、`Proto`=`https`、`Ssl`=`on`），不是可选开关，四个环境（test/dev/uat/prod）统一生效，因为都走同一份`chart_templates/`。
> 3. `ingress.annotations`（nginx专用的rewrite-target注解块）已从`template_{java8,newexchange_java8,java17_maven}.values.yaml`里删除；`rewrite_target`这个JSON字段/sed开关已从所有Jenkinsfile和`setting.groovy`里退休，因为apisix原生机制不需要它，也从没起过作用。
> 4. `websocket_port`跟路由无关，维持原状——它只控制`deployment.yaml`/`service.yaml`里是否存在websocket端口，不影响`apisixroute.yaml`。
> 5. **遗留提醒**：以后如果真的需要做路径前缀改写（比如同一域名下`/demo`转发要去掉`/demo`前缀），当前`apisixroute.yaml`的`proxy-rewrite`插件配置只做header改写，没有`uri`字段——需要单独扩展模板或新建`ApisixPluginConfig`，届时拿到真实需求例子再补，`ingress_paths`目前一律按普通前缀路径配置（如`/demo`）即可。
> 6. **额外发现（超出本次范围，仅供参考）**：`account-gateway`/`quotes-api`/`cloud-gateway`/`risk-engine`所在namespace（`uat-funds`/`uat-market`/`uat-product`/`uat-risk`）是业务线命名空间格式，跟本仓库`namespaces`服务级覆盖字段的命名习惯（如`uat-match`示例）一致——如果这几个服务本身也是靠`astrox-helm-chart`部署的，它们目前的域名路由是靠手写的独立`ApisixRoute`资源在管，不是靠本仓库chart生成的资源。这几个服务切到本次新的`apisixroute.yaml`自动生成机制后是否会跟手写资源冲突（重名/重复路由），未核实，不在本次处理范围内，接入前需要用户自行确认。

---

## 八、接入一个新服务（运维操作）

1. 在 `test/setting.groovy` 新增一个子字典（build字段+部署字段写在一起，因为test构建job和test部署job共用同一份文件同一个key），key 与即将创建的 test 构建/部署job同名：
   ```groovy
   "demo-svc": {
       "github_url": "https://github.com/your-org/astrox-demo.git",
       "project_type": "java8",
       "app_name": "demo-svc",
       "project": "astrox-demo",
       # 下面是部署字段，test部署job要用（新增）：
       "replicas": "1",
       "http_port": "8080",
       "actuator_port": "8081",
       "ingress_hosts": "demo-test.astroxs.com",
       "ingress_paths": "/",
   },
   ```
2. 在 `dev/setting.groovy`（以及要发布到的 uat）新增同名子字典（部署字段）：
   ```groovy
   "demo-svc": {
       "app_name": "demo-svc",
       "project": "astrox-demo",
       "project_type": "java8",
       "skywalking_enabled": "true",
       "replicas": "1",
       "http_port": "8080",
       "actuator_port": "8081",
       "ingress_hosts": "demo.astroxs.com",
       "ingress_paths": "/",  # 示例用普通前缀，别用 nginx 那种 /demo(/|$)(.*) 捕获组写法——ApisixRoute的proxy-rewrite目前只做header改写，不支持这种路径捕获组
   },
   ```
3. 提交 push 到 `astrox-helm-chart` 仓库（流水线每次会重新 clone 最新配置）。
4. 在 Jenkins 上创建 `demo-svc` 对应的 test 构建job（指向 `test/astrox.Jenkinsfile`）、test 部署job（指向 `test/deploy.Jenkinsfile`，新增）和各环境的部署job（指向 `<env>/astrox.Jenkinsfile`）。
5. 跑发布（见下一节）。

> 内部服务（不对外）记得加 `"no_ingress": "true"` 并留空 ingress 字段。

---

## 九、发布操作步骤

1. 先跑 **test 构建job**（`test/astrox.Jenkinsfile`）：Build with Parameters，选 `BRANCH_TAG`，构建完会push一份镜像到ECR（日志「Print image tag」步骤仍会打印tag，方便人工核对/需要指定历史版本时查）。
2. 再跑对应环境（**test部署job（`test/deploy.Jenkinsfile`，新增）**/dev/uat）的 **部署job**：Build with Parameters，`SPECIFY_TAG` **不勾选直接构建即可**——`Resolve image tag`阶段会自动去ECR查这个服务最新一次push的tag并部署，不用再手动复制。只有要部署/回滚到某个历史tag时才勾选`SPECIFY_TAG`并填`IMAGE_TAG`。
3. 日志末尾出现 `发布成功` 即完成；出现 `发布失败` 则流水线已自动回滚（见下一节）。
4. 校验：`kubectl get pods -n <ns> -l appname=<app_name>`，确认 Pod `Running`/`Ready`。
5. **prod** 不需要分两步，直接跑 prod 的job（自己 build+deploy），选 `BRANCH_TAG`/`Tag`。

---

## 十、回滚

### 10.1 自动回滚（流水线内置）
最后一个阶段 `kubectl rollout status` 在 300 秒（dev 380 秒）内未就绪即判定失败，流水线会自动：打印失败容器日志（kubetail）→ 执行 `kubectl rollout undo deployment <app>` 回滚到上一个版本。**无需人工介入。**

### 10.2 手动回滚（kubectl，推荐）
```bash
# 1. 查看历史版本
kubectl --kubeconfig <KUBECONFIG> rollout history deployment <app_name> -n <ns>

# 2. 回滚到上一个版本
kubectl --kubeconfig <KUBECONFIG> rollout undo deployment <app_name> -n <ns>

# 3. 回滚到指定版本
kubectl --kubeconfig <KUBECONFIG> rollout undo deployment <app_name> -n <ns> --to-revision=<N>

# 4. 观察回滚结果
kubectl --kubeconfig <KUBECONFIG> rollout status deployment <app_name> -n <ns>
```
> Deployment 的 `revisionHistoryLimit: 10`，最多可回退到最近 10 个版本。

### 10.3 手动回滚（helm）
```bash
helm history <app_name> -n <ns>              # 查看 release 历史版本
helm rollback <app_name> <REVISION> -n <ns>  # 回滚到指定 release 版本
helm status <app_name> -n <ns>
```

### 10.4 回滚方式如何选
- **发布刚失败、想退回上个好版本** → kubectl `rollout undo`（最快，且与流水线自动回滚一致）。
- **要回到更早的某个明确版本** → kubectl `--to-revision` 或 helm `rollback <REVISION>`。
- **改坏了 values/模板想整体退回，或想退回到某个更早的 IMAGE_TAG** → 重新跑一次部署job，勾选`SPECIFY_TAG`并把`IMAGE_TAG`填回老版本的tag（部署job本身是幂等的，不需要重新 build）。

---

## 十一、常用运维命令

```bash
# Pod 状态 / 详情 / 日志
kubectl get pods -n <ns> -l appname=<app_name>
kubectl describe pod <pod> -n <ns>
kubectl logs -f <pod> -n <ns>
kubetail -n <ns> -l appname=<app_name>        # 聚合多副本日志

# 进容器排障
kubectl exec -it <pod> -n <ns> -- sh

# Helm release
helm list -n <ns>
helm get values <app_name> -n <ns>            # 看实际生效的 values

# 扩缩容（临时）
kubectl scale deployment <app_name> -n <ns> --replicas=<N>
```

---

## 十二、关键模板说明（deployment.yaml）

- **kind**：按 `values.kind` 渲染为 `Deployment` 或 `StatefulSet`。
- **滚动发布**：`progressDeadlineSeconds: 200`、`revisionHistoryLimit: 10`。
- **健康检查**：liveness/readiness 均为 `tcpSocket` 探针，`initialDelaySeconds: 60`、`failureThreshold: 20`、`periodSeconds: 10`。新服务启动慢时注意这套阈值。
- **SkyWalking**：`skywalking_enabled=true` 时加 initContainer 注入 java agent，并注入 `SW_*` 环境变量与日志注解。
- **存储**：`nfs_server` 挂载 `/data/config`（只读配置），`log_nfs_server` 挂载 `/data/log`（日志），均来自 EFS。
- **调度**：`nodeSelector`（environment 标签）+ `tolerations`（按 namespace 容忍 NoExecute）+ `podAntiAffinity`（同名服务尽量分散到不同节点）。
- **HPA**：`hpa.yaml` 受 `autoscaling` 控制，prod 默认开启。

---

## 十三、注意事项与易踩坑

1. **任务名必须等于配置 key**：Jenkins 任务名与 `setting.groovy` 的子字典 key 不一致会直接 `exit 1`。
2. **改配置即生效**：流水线每次重新 `git clone` 本仓库，改完 `setting.groovy` 必须 push 才会生效。
3. **prod 与 dev 的差异**：镜像仓库、namespace（`prod25`）、KUBECONFIG、EFS 区域（us-west-2）、资源规格、autoscaling 默认开启都不同，发布 prod 前确认改的是 `prod/` 目录。
4. **dev/uat 部署前必须先跑 test 构建**：没跑过 test，ECR里没有这个服务的镜像，`SPECIFY_TAG`不勾选走自动解析会失败并报错退出（`aws ecr describe-images` 查不到任何tag）；日常部署`SPECIFY_TAG`不需要勾选，走自动取最新tag逻辑，勾选并手动填`IMAGE_TAG`仅用于回滚/指定历史版本。
5. **KUBECONFIG 来源**：由 Jenkins 凭据托管，用 `--kubeconfig ${KUBECONFIG}`。
6. **内部服务**：不暴露的服务务必设 `no_ingress=true`，否则会生成无效的 `ApisixRoute`。
7. **健康检查阈值**：探针 `initialDelaySeconds 60s`，启动特别慢的服务可能在 `rollout status` 超时窗口内被判失败并自动回滚，必要时调大超时或探针阈值。
8. **GitHub 凭据是占位符**：仓库里 `pipline.groovy` 的 `GITHUB_CREDENTIALS_ID_PLACEHOLDER`（以及 `clone helm chart` 阶段沿用的旧凭据ID `9bb9a583-a510-4e45-91cc-bd4a3b9c307d`）都需要在真正部署前，换成 Astrox 自己在 Jenkins 里配置的真实凭据ID，否则 checkout 会失败。
9. **Jenkins 地址切换不在本仓库范围**：切到 `https://jenkins.astroxs.com/` 是 Jenkins 侧的 job/服务器配置，本仓库代码没有硬编码任何 Jenkins 地址，不需要因此改代码。
10. **`env_tier` 和 `namespaces` 别弄混**：改服务的k8s namespace（业务线隔离）只需要改 `namespaces`，改完chart源码目录路径不受影响（还是走 `env_tier`）；`env_tier` 本身不可通过服务字典覆盖，改错了会导致读不到chart目录。
11. **uat镜像跨账号pull前提**：uat部署前必须确认AWS侧ECR仓库策略已经放开给uat账号（`696000197734`）跨账号权限，至少包括`ecr:GetDownloadUrlForLayer`/`ecr:BatchGetImage`（拉镜像）和`ecr:DescribeImages`（`SPECIFY_TAG`不勾选时自动查最新tag要用），否则镜像拉取或自动取tag会失败（详见第三节）。
14. **`IMAGE_TAG`自动解析查的是test构建时实际push的ECR路径**：dev/uat的`astrox.Jenkinsfile`会额外读一次`test/setting.groovy`，取该服务在test环境下的`namespaces`覆盖值（不是字面量`"test"`，也不是dev/uat自己的`namespaces`）来拼ECR仓库名——如果只改了`dev/setting.groovy`没同步改`test/setting.groovy`里这个服务的`namespaces`，会导致自动取tag查到错误/不存在的仓库路径。
12. **飞书webhook地址是占位符**：各环境 `setting.groovy` 的 `lark_webhook_url` 目前是 `FILL_IN_LARK_WEBHOOK_URL`，需要去飞书群机器人设置里核实真实地址后替换，否则通知步骤会静默失败（代码里 `|| true` 兜底，不影响主流程，但收不到通知）。
13. **buildah 不是 docker**：新构建流程用 `buildah --storage-driver=vfs`，agent容器镜像必须内置 `buildah`/`aws` CLI，且push前显式走 `aws ecr get-login-password | buildah login`（不像原来 docker 依赖 `docker-credential-ecr-login` 免登录）。
15. **test的`deploy_agent_image`是占位符**：`test/setting.groovy`的`private.deploy_agent_image`目前是`FILL_IN_DEPLOY_AGENT_IMAGE_WITH_HELM_KUBECTL_AWSCLI`，部署前必须替换成真实的、内置`helm`/`kubectl`/`aws` CLI的镜像地址——注意这个镜像跟test构建job用的`private.agent_image`（JDK17/Maven/buildah规格）不是同一个，别填错。
16. **`chart_templates/`改了要等下次部署才生效**：`template.Chart.yaml`/`template_*.values.yaml`/`templates/*`现在只在仓库根目录`chart_templates/`维护一份，各环境部署job在`Update values.yaml`阶段运行时才会拷贝一份到自己环境目录——这意味着改了`chart_templates/`不会立刻影响任何正在跑的环境，必须等对应服务下一次跑部署job才会应用新的chart结构；也意味着**不要**再去`dev/uat/prod`目录下手动创建/编辑`template_*.values.yaml`或`templates/`，那些文件已删除，编辑了也不会被使用（会被下次部署的拷贝步骤覆盖）。

---

*文档依据仓库当前代码整理，后续模板或流水线变更后请同步更新本文。*
